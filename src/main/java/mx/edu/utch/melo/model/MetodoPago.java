package mx.edu.utch.melo.model;

/** Métodos de pago aceptados (ver CLAUDE.md). Una orden puede tener varios (división de cuenta). */
public enum MetodoPago {
    EFECTIVO,
    TARJETA,
    TRANSFERENCIA
}

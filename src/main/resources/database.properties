# Configuracion de conexion a MySQL leida por mx.edu.utch.melo.db.ConexionDB.
# Ajusta estos valores a tu entorno local. En un entorno real este archivo
# no deberia llevar credenciales reales al repositorio (usar variables de
# entorno o un .properties fuera de control de versiones).

db.url=jdbc:mysql://localhost:3306/melo_pos?useSSL=false&serverTimezone=UTC
db.usuario=root
db.contrasena=
